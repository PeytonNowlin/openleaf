/**
 * Deep immutability, re-exported from the shared package.
 *
 * Same shape as `css.ts`, `embed.ts` and `url.ts`: one definition lives in
 * `@openleaf-editor/content-policy` and every consumer imports it.
 */
export { deepFreeze } from '@openleaf-editor/content-policy/freeze'
